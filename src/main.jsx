import React from 'react'
import ReactDOM from 'react-dom/client'
import MalariaSimulator from './MalariaSimulator.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MalariaSimulator />
  </React.StrictMode>,
)
